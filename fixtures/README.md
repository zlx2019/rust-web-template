# Fixtures

Fixture data for unit and integration tests. Keep entries small, deterministic
and committed to version control so tests stay reproducible.

Typical contents:

- Sample request / response bodies (JSON, form data, etc.).
- Configuration files loaded by tests.
- Golden files for snapshot assertions.
